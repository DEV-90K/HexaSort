public static class GameConstants
{     
    public static class HexagonConstants
    {
        public const float TIME_ANIM = 0.2f * 2; //0.2f
        public const float TIME_DELAY = 0.03f * 2;//0.01f
        public const float HEIGHT = 0.2f;
        public const float HEIGHT_BONUS = 2f; //Height jump merger bonus
    }

    public static class StackHexagonConstants
    {
        public const float CONTACT_HEIGHT = 1.5f; //10 * 0.2f;
    }
}
